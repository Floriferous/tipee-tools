# dependencies
node_modules/
.pnpm-store/

# build output and task cache
dist/
.turbo/

# Tipee API keys live here — local only, never committed
.env
packages/*/.env

# logs
*.log

# editors and OS
.DS_Store
.idea/
.claude/worktrees/

# opensrc source mirror for exploration (symlink into the global cache)
opensrc/
