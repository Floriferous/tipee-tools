{
  "name": "@tipee-tools/cli",
  "version": "0.0.1",
  "description": "Read-only command line for the Tipee HR API",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "tipee": "node --env-file-if-exists=.env src/main.ts"
  },
  "dependencies": {
    "@tipee-tools/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "msw": "^2.15.0",
    "vitest": "^5.0.0"
  }
}
